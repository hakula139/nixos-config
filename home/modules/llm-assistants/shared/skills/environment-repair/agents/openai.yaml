interface:
  display_name: "Environment Repair"
  short_description: "Delegate fixes for local assistant environment failures"
  default_prompt: "Use $environment-repair to delegate a local fix for this reproducible assistant environment failure."
